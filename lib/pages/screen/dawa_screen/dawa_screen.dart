import 'package:flutter/material.dart';

class DawaScreen extends StatelessWidget {
  const DawaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('DawaScreen'),
      ),
    );
  }
}
